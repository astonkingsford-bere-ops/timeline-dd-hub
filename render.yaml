services:
  - type: web
    name: timeline-dd-hub
    env: node
    nodeVersion: 20.11.0
    buildCommand: npm install && npm run build
    startCommand: node api/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SERP_API_KEY
        sync: false
